@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Ocurrió un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo Desarrollo</h3>
<p>
    Cambiar al entorno <strong>Desarrollo</strong> mostrará información más detallada sobre el error ocurrido.
</p>
<p>
    <strong>El entorno de Desarrollo no debe estar habilitado en aplicaciones desplegadas.</strong>
    Puede mostrar información sensible de las excepciones a los usuarios finales.
    Para depuración local, habilite el entorno <strong>Desarrollo</strong> configurando la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> en <strong>Development</strong>
    y reiniciando la aplicación.
</p>
